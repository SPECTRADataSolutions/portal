# // www.spectradatasolutions.com – For support, contact Mark at this URL.
name: "Analyse Python Code"

on:
  push:
    branches: [main, develop]
    paths:
      - "**/*.py"
      - "pyproject.toml"
  pull_request:
    branches: [main, develop]
    paths:
      - "**/*.py"
      - "pyproject.toml"

jobs:
  analyze-python-code:
    name: "Analyse Python Code"
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install ruff

      - name: Run Ruff linter
        run: ruff check --output-format=github .

      - name: Run Ruff formatter check
        run: ruff format --check .

      - name: Show analysis summary
        if: always()
        run: |
          echo "::notice::Python code analysis completed with Ruff."
          echo "For Ruff documentation, see: https://docs.astral.sh/ruff/"
